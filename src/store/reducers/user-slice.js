import { createSlice } form "@reduxjs/toolkit"

const iniState = {
  isLogging: false,
  isLogOn: false,
  data: {} 
}

const userSlice = createSlice({
  name: "user"
  intialState,
  reducer:
  // sync

  // async
})